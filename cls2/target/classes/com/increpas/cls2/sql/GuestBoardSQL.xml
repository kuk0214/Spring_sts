<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybaits.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="gSQL">
 	 <!-- 방명록 글 리스트 조회 질의명령 -->
 	 <select id="gBoardList" resultType="bVO">
 	 	SELECT
 	 		gno, mno, id, savename avatar, body, wdate
 	 	FROM
 	 		guestboard, avatar, member
 	 	WHERE
 	 		writer = mno
 	 		AND avt = ano
 	 		AND guestboard.isShow = 'Y'
 	 </select>
 	 
 	 <!-- 방명록 카운트 조회 질의명령 -->
 	 <select id="writeCount" resultType="int" parameterType="int">
 	 	SLECT
 	 		COUNT(*) cnt
 	 	FROM
 	 		guestboard
 	 	WHERE
 	 		writer = #{mno}
 	 </select>
</mapper>